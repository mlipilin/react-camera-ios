/* eslint-disable react/jsx-filename-extension */
import Camera from './components/Camera';
import { DEVICE, PLACEMENT, FACING_MODE } from './constants';

export { DEVICE, PLACEMENT, FACING_MODE };
export default Camera;
